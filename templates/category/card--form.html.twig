<div class="card bg-light my-4 mx-auto" style="max-width: 30rem">
	<div class="card-header h2">
		{% if currentCategory.name == null %}Création d'une{% else %}Modification de la
		{% endif %}
		catégorie
	</div>
	<div class="card-body">

		{{ form_start(form) }}
		{{ form_row(form._token) }}

		<div class="row">
			<div class="col-7">
				{{ form_label(form.name)}}
				{{ form_widget(form.name, {'attr': {'class': 'form-control', 'value': currentCategory.name, 'placeholder': "Nom de la catégorie"} })}}
			</div>
			<div class="col-3">
				{{ form_label(form.sort)}}
				{{ form_widget(form.sort, {'attr': {'class': 'form-control', 'value': currentCategory.sort, 'placeholder': "N°"} })}}
			</div>
			<div class="col-2">
				{{ form_label(form.trash)}}
				{{ form_widget(form.trash, {'attr': {'class': 'form-check', 'checked': currentCategory.trash} })}}
			</div>
		</div>

		<div class="d-flex justify-content-around mt-4">
                <button class="submit btn btn-success">Enregistrer</button>
            {% if currentCategory.name != null %}
                <a href={{ path('admin_category_delete', {slug: currentCategory.slug} ) }} class="btn btn-primary">Supprimer</a>
                <a href={{ path('admin_category_index') }} class="btn btn-secondary">Annuler</a>
    		{% endif %}
		</div>

	</div>
</div>
