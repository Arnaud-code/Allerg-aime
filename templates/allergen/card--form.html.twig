<div class="card bg-light my-4 mx-auto" style="max-width: 30rem">
	<div class="card-header h2">
		{% if currentAllergen.name == null %}Création d'un{% else %}Modification de l'
		{% endif %}
		allergène
	</div>
	<div class="card-body">

		{{ form_start(form) }}
		{{ form_row(form._token) }}

		<div class="row">
			<div class="col-9">
				{{ form_label(form.name)}}
				{{ form_widget(form.name, {'attr': {'class': 'form-control', 'value': currentAllergen.name, 'placeholder': "Nom de l'allergène"} })}}
			</div>
			<div class="col-3">
				{{ form_label(form.sort)}}
				{{ form_widget(form.sort, {'attr': {'class': 'form-control', 'value': currentAllergen.sort, 'placeholder': "N°"} })}}
			</div>
		</div>

		<div>
			{{ form_label(form.description)}}
			{{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': "Description de l'allergène"}, 'value': currentAllergen.description })}}
		</div>

		<div class="d-flex justify-content-around mt-4">
                <button class="submit btn btn-success">Enregistrer</button>
            {% if currentAllergen.name != null %}
                <a href={{ path('admin_allergen_delete', {slug: currentAllergen.slug} ) }} class="btn btn-primary">Supprimer</a>
                <a href={{ path('admin_allergen_index') }} class="btn btn-secondary">Annuler</a>
    		{% endif %}
		</div>

	</div>
</div>

